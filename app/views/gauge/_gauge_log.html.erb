<tr id=<%= "gauge-log-row#{gauge_log.id}" %>>
  <%= render partial: 'gauge_log_manager_version', locals: { gauge_log: gauge_log } if profile.is_manager? %>
  <%= render partial: 'gauge_log_employee_version', locals: { gauge_log: gauge_log } if profile.is_employee? %>
</tr>